{% extends 'default/default.html.twig' %}

{% block content %}
    {{ page.content }}
    {% include 'default/components/carousel.html.twig' %}
    {% for module in page.collection() %}
        {% if module.header.title|contains('Slider')%}
        {% else %}
            {{ module.content }}
        {% endif %}
    {% endfor %}
{% endblock %}
